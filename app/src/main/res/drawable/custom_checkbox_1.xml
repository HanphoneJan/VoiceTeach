<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 选中状态，整个背景变为蓝色 -->
    <item android:state_checked="true">
        <shape
            android:shape="rectangle">
            <stroke
                android:width="1dp"
                android:color="@color/grey_100" /> <!-- 边框宽度和颜色 -->
            <corners android:radius="48dp" /> <!-- 圆角半径 -->
            <solid android:color="@color/light_blue_100" /> <!-- 背景颜色，与 CheckBox 原背景色保持一致 -->
        </shape>
    </item>
    <!-- 未选中状态，背景透明 -->
    <item android:state_checked="false">
        <shape
            android:shape="rectangle">
            <stroke
                android:width="1dp"
                android:color="@color/grey_100" /> <!-- 边框宽度和颜色 -->
            <corners android:radius="48dp" /> <!-- 圆角半径 -->
            <solid android:color="@color/grey_100" /> <!-- 背景颜色，与 CheckBox 原背景色保持一致 -->
        </shape>
    </item>

</selector>